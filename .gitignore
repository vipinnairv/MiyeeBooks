# dependencies
node_modules/

# build output (generated by build.mjs; CI rebuilds it for GitHub Pages)
dist/

# one-time migration artifact - the original monolith. Kept locally so the
# splitter can be re-run; the full history lives in git, so it isn't tracked.
index.legacy.html
